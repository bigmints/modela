services:
  modela:
    build: .
    user: "${MODELA_UID:-1000}:${MODELA_GID:-1000}"
    working_dir: /app
    ports:
      - "127.0.0.1:4173:4173"
    volumes:
      - .:/app
    environment:
      MODELA_INCLUDE_PAID_OPENROUTER: "${MODELA_INCLUDE_PAID_OPENROUTER:-false}"
      MODELA_MAX_DROP_RATIO: "${MODELA_MAX_DROP_RATIO:-0.5}"
    restart: "no"
    command: ["update"]
